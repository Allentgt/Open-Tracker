<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".CalorieTrackerApp"
        android:allowBackup="true"
        android:icon="@mipmap/app_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/app_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.IndianCalorieTracker">
        
        <!-- Auth0 SDK requires these manifest placeholders -->
        <meta-data
            android:name="auth0Domain"
            android:value="${auth0Domain}" />
        <meta-data
            android:name="auth0Scheme"
            android:value="com.indiancalorietracker" />
        
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.IndianCalorieTracker"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Auth0 callback intent filter -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="com.indiancalorietracker"
                    android:host="dev-allentgt.us.auth0.com"
                    android:pathPrefix="/android/com.indiancalorietracker/callback" />
            </intent-filter>
        </activity>
    </application>

</manifest>
